<div class="container">
	<div class="row row-offcanvas row-offcanvas-right">
		<div class="col-xs-12 col-sm-9">
			<div class="row">
            		<div class="col-sm-9">
					<h5><em><u><strong>Displaying most recent posts:</strong></u></em></h5>
					<% if Post.exists?(@first_post)%>
						<div id="posts_list">
							<%= render '/posts/all_posts_list' %>
						</div>
					<% else %>
					If you're seeing this, it's because there are no posts. You can start by creating a topic, and then creating a post within that topic. All posts from all topics will populate on this home page in chronological order.
					<% end %>
				</div>
			</div>
		</div>
		<div class="col-xs-6 col-sm-3 sidebar-offcanvas" id="sidebar">
			<div class="container-fluid">
				<div class="input-group">
	      			<input type="text" class="form-control" placeholder="search">
	      			<span class="input-group-btn">
	        				<button class="btn btn-default" type="button">Go!</button>
	      			</span>
	    			</div>
	    		</div>
	    	</div>
	</div>
</div>